name: tessl
description: Agent enablement platform - package manager for AI agent skills
entrypoint: tessl
default_version: latest
dependencies:
  - claude
mounts:
  - source: ~/.tessl
    target: /home/dclaude/.tessl
  - source: ~/.config/tessl
    target: /home/dclaude/.config/tessl
